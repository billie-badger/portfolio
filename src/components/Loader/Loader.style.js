import styled from 'styled-components';

export const LoadingImg = styled.div`
    position: relative;
    display: block;
    margin: 0 auto;
    background-image: url('${require('../../assets/loader.gif')}');
    width: 50%;
    height: 50%;
`;